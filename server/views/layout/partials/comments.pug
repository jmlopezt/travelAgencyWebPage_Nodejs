blockquote.text-center Read about our users's experiences
.row.comments
    if(comments)
        each comment in comments
            .col-md-6.col-lg-4.mb-4
                .card
                    .card-body
                        blockquote.blockquote
                        p.mb-0
                            | #{comment.message}
                        footer.blockquote-footer
                            | #{comment.name}